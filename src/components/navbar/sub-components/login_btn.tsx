import { Button } from "react-bootstrap";
import React from "react";


    const LoginBtn = () => {
        return (
            <div>
                <Button variant="outline-dark">Log In</Button>
            </div>
        );
    }

    export {LoginBtn};